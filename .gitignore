node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.amplify-hosting
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# generated sample PDFs
sample-output/

# Working notes for this repo's maintainer — deliberately untracked (the repo
# is public; CLAUDE.md carries AWS account details and cross-project context).
CLAUDE.md

# Output of the legacy Python scripts — regenerable from legacy/badges/*.py.
# genealogy.pdf is kept as the layout reference the README compares against.
legacy/output/*
!legacy/output/genealogy.pdf

# Scraped merit badge corpus from scouting.org — official BSA content that this
# public repo deliberately does not republish. Regenerate with `pnpm scrape:badges`.
data/

# The troop's own watermark. Kept local rather than published with the tool —
# render-badges.ts treats its absence as normal and renders unmarked.
assets/watermark.*
